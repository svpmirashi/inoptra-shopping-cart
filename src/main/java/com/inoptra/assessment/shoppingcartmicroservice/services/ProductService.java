package com.inoptra.assessment.shoppingcartmicroservice.services;

import java.util.HashMap;
import java.util.List;
import java.util.function.Function;
import java.util.stream.Collectors;

import org.springframework.stereotype.Service;

import com.inoptra.assessment.shoppingcartmicroservice.models.ProductItem;

@Service
public class ProductService {
//    public void processProducts(List<ProductItem> productItems) {
//        productItems.stream().collect(Collectors.groupingBy(Function.identity(), Collectors.counting()))
//    }
}
